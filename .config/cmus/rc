colorscheme zenburn

set mouse=true

set lib_sort=artist album discnumber tracknumber title filename

# Missing page up/ page down bindings
bind -f common ^D win-page-down
bind -f common ^U win-page-up


bind -f common  < seek -30
bind -f common  > seek +30
