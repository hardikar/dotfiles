# start screen at window 1 and bind window 0 to 10 
bind c screen 1
bind ^c screen 1
bind 0 select 10                                                            
screen 1

# always show caption bar (usually only visible when in splitscreen mode)
caption always
